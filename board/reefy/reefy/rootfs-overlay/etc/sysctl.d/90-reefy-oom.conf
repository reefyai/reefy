# Reserve 128MB free so OOM killer fires before system thrashes to death.
# Default ~66MB is too small — on no-swap systems the kernel enters a
# kswapd thrash spiral before triggering OOM, and the hardware watchdog
# reboots the device before the OOM killer can act.
vm.min_free_kbytes = 131072
