# /etc/nsswitch.conf
# No nss_systemd: it crashes when dlopen'd into static BusyBox (glibc NSS + static binary).
# DynamicUser=, systemd-homed, systemd-machined are not used.

passwd:         files
group:          files
shadow:         files

hosts:          files dns
networks:       files dns

protocols:      files
services:       files
ethers:         files
rpc:            files
