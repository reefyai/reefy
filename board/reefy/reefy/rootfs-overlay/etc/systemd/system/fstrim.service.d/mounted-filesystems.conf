[Service]
# App volumes are mounted dynamically, not listed in /etc/fstab. The upstream
# --listed-in fallback stops at our nonempty fstab and misses these filesystems.
ExecStart=
ExecStart=/usr/bin/fstrim --all --verbose --quiet-unsupported
