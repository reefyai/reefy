[Manager]
# Hardware watchdog: systemd pings /dev/watchdog0 every 60s.
# If systemd hangs (kernel panic, deadlock) → hardware reboots after 120s.
RuntimeWatchdogSec=120
# If shutdown hangs → hardware force-reboots after 2 minutes.
RebootWatchdogSec=2min
